const db = require("../models");
const User = db.User;
const Email = db.Email;
const Channel = db.Channel;

// Thêm kênh mới
const addNew = (req, res, next) => {};

// Chỉnh sửa thông tin kênh
const editChannelInfo = (req, res, next) => {};

// Xóa kênh
const deleteChannel = (req, res, next) => {};
